import type { CSSProperties, MouseEvent } from "react";

/** Inline commenting is disabled in the Codex selection experiment. */
export function HoverCommentButton(_props: {
  onClick: (event: MouseEvent<HTMLButtonElement>) => void;
  className?: string;
  style?: CSSProperties;
}) {
  return null;
}
